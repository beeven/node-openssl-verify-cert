language: node_js

node_js:
- 4.2.4

os:
- linux

addons:
    apt:
        sources:
        - ubuntu-toolchain-r-test
        packages:
        - gcc-4.9
        - g++-4.9

before_install:
- export CC="gcc-4.9" CXX="g++-4.9"

before_script:
- npm install -g mocha

script:
- npm test
